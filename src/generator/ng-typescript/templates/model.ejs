export interface I<%= model.name %> {
	<%_ for (const prop of model.properties) { -%>
	<%= prop.name %><%= !prop.required ? '?' : '' %>: <%= prop.type %><%= prop.nullable ? ' | null' : '' %>;
	<%_ } -%>
}
