export enum <%= name %> {
	<%_ for (const entry of entries) { -%>
	<%_ if (isStringlyTyped) { -%>
	<%= entry.name %> = '<%= entry.value %>',
	<%_ } else { -%>
	<%= entry.name %> = <%= entry.value %>,
	<%_ } -%>
	<%_ } -%>
}
